<androidx.swiperefreshlayout.widget.SwipeRefreshLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/swipeRefreshLayout2"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- ListView -->
        <ListView
            android:id="@+id/list_view2"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:divider="@color/colorPrimaryLight"
            android:dividerHeight="1dp" />

        <!-- Export Button -->
        <androidx.appcompat.widget.AppCompatImageButton
            android:id="@+id/exportButton2"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:layout_alignParentBottom="true"
            android:layout_alignParentEnd="true"
            android:layout_marginBottom="16dp"
            android:layout_marginEnd="16dp"
            android:src="@drawable/save"
            android:contentDescription="Dışa Aktar"
            android:background="?attr/selectableItemBackgroundBorderless" />

        <!-- Whistle Button (üstte dursun) -->
        <androidx.appcompat.widget.AppCompatImageButton
            android:id="@+id/whistleButton2"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:layout_above="@id/exportButton2"
            android:layout_alignEnd="@id/exportButton2"
            android:layout_marginBottom="16dp"
            android:src="@drawable/ic_whistle"
            android:contentDescription="Acil Düdük"
            android:scaleType="centerInside"
            android:background="?attr/selectableItemBackgroundBorderless" />
    </RelativeLayout>

</androidx.swiperefreshlayout.widget.SwipeRefreshLayout>